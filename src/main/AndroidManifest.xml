<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartcontacts">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.SlideMenuActivity"/>
        <activity android:name=".ui.activity.ParallaxActivity"/>
        <activity android:name=".ui.activity.SwipeMenuActivity"/>
        <activity android:name=".ui.activity.GooActivity"/>
        <activity android:name=".ui.activity.TestActivity"/>

    </application>

</manifest>